import React from "react";

export default function Header(){
    return(
        <header>
            <h1 className="p-3 sm:rounded-t-lg bg-gradient-to-r from-cyan-500 to-blue-500 text-white">Random Chooser</h1>
        </header>
    );
}